[project]
name = "week-1"
version = "0.1.0"
description = "SEDS Celestia - Rigid Ball Collisions"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "pygame-ce>=2.5.0",
    "numpy>=1.24.0",
]

[project.scripts]
week-1 = "week_1:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"